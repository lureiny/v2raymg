# Code Review — 2026-04-09

全量 review，覆盖 `pkg/proxy/` 重构模块。共 7 个 session，按包/层次划分。

每条问题标注：**优先级**（P0/P1/P2/P3）、文件路径和行号、具体描述。

优先级定义：
- **P0**：正确性/安全性错误，需立即修复
- **P1**：高优先级稳定性/设计缺陷
- **P2**：中优先级一致性/接口设计问题
- **P3**：低优先级清理/规范问题

---

## S1 — errors + tools/process

### `pkg/proxy/errors/errors.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| E-01 | P1 | 132–142 | `ProxyError.Is` 中 `any(target).(ErrorCode)` 类型断言永远不成功（ErrorCode 不实现 error 接口），"backward compatibility" 路径是死代码 |
| E-02 | P1 | 220–222 | `ToError()` 返回标准库 `errors.New`，与 `ProxyError` 的 `errors.Is` 匹配完全失效；现有测试只测自我匹配，掩盖了这个缺陷 |
| E-03 | P2 | 214–216 | `HasCode` 只检查链头，不遍历整个 cause chain，与 `errors.Is` 语义不一致 |
| E-04 | P3 | 324–327 | `NewErrAt` 使用两个 `%w`（Go 1.20 特性），go.mod 声明 1.18+，在 1.18/1.19 下第二个 `%w` 静默退化为 `%v` |
| E-05 | P3 | 225–292 | 9 个错误码（`ErrContainerStartFailed`、`ErrFileNotFound` 等）在 `Message()` switch 中没有 case，fallback 返回原始 code 字符串 |

### `pkg/proxy/tools/process/runner.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| P-01 | P1 | 132–136 | `IsRunning()` 用 `cmd.Process != nil` 判断，进程自行退出后仍返回 true（误报） |
| P-02 | P1 | 106–116 | `Stop()` 中 goroutine 闭包引用 `p.cmd`，在锁外并发访问，存在竞态 |
| P-03 | P2 | 96–102 | `Stop()` 发送 SIGINT 失败时返回 error，但进程已被 Kill 清理，上层会误判"停止失败" |
| P-04 | P2 | 124–129 | `Restart()` 两次加锁之间有空隙，并发调用可能导致双重 Start |
| P-05 | P2 | 26–29 | `Stdout`/`Stderr` 字段类型为 `interface{}`，Start 时做无保护类型断言，传入非 Writer 会 panic |
| P-06 | P3 | 61–64 | `-c` flag 硬编码（xray 惯例），与"通用进程执行器"定位矛盾 |
| P-07 | P3 | 150–154 | `Process()` 暴露内部 `*exec.Cmd`，调用方可绕过锁直接操作 |

**测试缺口**：`IsRunning` 进程自然退出场景未测；`TestRunner_Version` 是空测试无任何断言；缺少 `-race` 并发测试。

---

## S2 — core/contracts

### `pkg/proxy/core/contracts/protocol.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| C-01 | P1 | 27–34 | `AllProtocols()` 只返回 4 个协议，`IsValid()` 接受 8 个，两者不一致且无注释说明设计意图 |
| C-02 | P1 | 118–129 | `ValidateCombination()` 只做枚举校验，不检查跨字段组合（Reality+非VLESS、XTLS+gRPC 均不拦截），真正校验散落在 xray adapter 里 |
| C-03 | P3 | 55–57 | `AllTransports()` 未包含实际使用的 `xhttp`/`splithttp`/`h2`/`h3` 变体 |

### `pkg/proxy/core/contracts/inbound.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| C-04 | P1 | 10–23 | `ListenAddr` 是所有实现都需要的基础字段，却被迫放入 Extensions（key: `"listen_addr"`），丢失类型安全 |
| C-05 | P3 | 30 | 端口下限 100 无命名常量，在代码库中硬编码出现 3 次 |

### `pkg/proxy/core/contracts/user.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| C-06 | P2 | 14–103 | `UserSpec` 混合 7 个关注点（身份/配额/限速/连接限制/端口/集群同步/前端认证），`BindPorts`/`PortMappings` 是 forward 内部细节，`UpdatedAtUs`/`OriginNode`/`Hash` 是集群字段，污染核心用户模型 |
| C-07 | P2 | 21 vs 79 | `AuthToken`（UUID，可直接用于认证）序列化为 JSON，而 `LoginPassword`（bcrypt hash）用 `json:"-"`，敏感程度更高的反而暴露 |
| C-08 | P3 | 85 | `DeletionState` 字段类型是裸 `string`，允许赋任意值，无编译期约束 |

### `pkg/proxy/core/contracts/subscription.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| C-09 | P1 | 6–35 | `transport`/`security` 在所有 URI 格式中都是必需字段，却放在 Extensions 里（魔法字符串 key） |
| C-10 | P3 | 52 | `ExcludeProtocols` 类型为 `[]string` 而非 `[]Protocol`，丢失类型安全 |
| C-11 | P3 | 28–30 | `SubscriptionSpec.URI` 在同一 struct 中同时扮演输入和输出角色，语义模糊 |

### `pkg/proxy/core/contracts/container.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| C-12 | P2 | 5–29 | `ContainerModel` 在代码库中没有实际消费者，与 `ExecutorConfig` 职责高度重叠，应删除或标记为规划中 |
| C-13 | P3 | 33–38 | `Stats.Type`/`Stats.Proxy` 应为具名枚举类型 |

### 跨文件

| ID | 优先级 | 问题 |
|----|--------|------|
| C-14 | P1 | `contracts.InboundSpec` 和 `inbound.Config` 两套类型并存，结构几乎相同，无明确转换关系 |
| C-15 | P2 | Extensions 中 30+ 魔法字符串 key（`ws_path`、`reality_private_key` 等）无常量定义，分散在 3 个文件里 |
| C-16 | P2 | `Transport` 枚举值与 xray 内部表示不一致（`"http"` vs `"h2"`），映射逻辑散落在多处 |

---

## S3 — core/container + core/inbound 抽象层

### `pkg/proxy/core/inbound/`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| I-01 | P1 | default.go | `DefaultInbound` 有 `tag_`/`port_` 等独立字段，同时维护 `config_ *Config`，`SetTag()` 只更新前者不同步后者，`Tag()` 与 `Config().Tag` 可能不一致 |
| I-02 | P2 | inbound.go:70 | `Config()` 返回 `*Config` 裸指针，调用方可直接改写 Extensions，破坏封装 |
| I-03 | P2 | inbound.go:74 | `Extra()` 与 `Config().Extensions` 语义重叠，无文档说明两者区别 |
| I-04 | P2 | inbound.go:99–113 | `InboundError` 没有实现 `Is(target error) bool`，`errors.Is` 跨实例匹配失败 |
| I-05 | P3 | default.go:22–27 | `NewDefaultInbound` 对空 tag/零 port 静默替换默认值，与 `NewConfig()` 行为不一致 |

### `pkg/proxy/core/container/interface.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| CT-01 | P2 | 21 | `Init(config any)` 类型不安全，与 `Factory.New(BuildOptions)` 职责重叠，应合并或删除 |
| CT-02 | P2 | 37–38 | `GetRunFunc()` 是 BaseContainer 内部钩子机制，不应暴露为 Container 公开接口 |
| CT-03 | P2 | 65 | `GetUserSubscriptions` 强制所有 Container 实现，应改为可选能力（type assertion） |
| CT-04 | P2 | 55 | `FastAddInbound` 的 `params map[string]any` 仅靠注释约束必填 key，应定义结构体 |

### `pkg/proxy/core/container/base.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| CT-05 | P1 | 179–195 | Starting→Running 之间释放锁，另一 goroutine 调用 Start 会误认为启动成功，实际 runFunc 还在执行 |
| CT-06 | P1 | 239–248 | Stop 在 Starting 状态时立即 MarkStopped，但 runFunc 可能还在跑，进程成为孤儿（无 stopFunc 记录） |
| CT-07 | P3 | 297 | `Restart()` 中额外调用 `MarkRunning()` 是多余的（Start 内部已调用） |
| CT-08 | P3 | 156–162 | `stopChan` 每次 Start 重建，Restart 后持有旧引用的调用方永远不会收到关闭信号 |

### `pkg/proxy/core/container/registry.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| CT-09 | P1 | 13–217 | 两套注册机制并存（旧：`globalRegistry`/`RegisterContainer`；新：`factoryMap`/`RegisterFactory`），`ContainerMgr` 只用新机制，旧机制注册对 ContainerMgr 完全无效，但无任何警告 |
| CT-10 | P3 | 104–116 | 两套机制锁顺序在不同调用路径上不一致，是后续死锁隐患 |

### `pkg/proxy/core/container/manager.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| CT-11 | P2 | 54–85 | `LoadFromConfig` 重复加载同一 ContainerType 会静默覆盖运行中的实例，旧实例不被 Stop，资源泄漏 |

### `pkg/proxy/core/container/factory.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| CT-12 | P3 | 25 | `BuildOptions.Config any` 应为 `ContainerConfig` 接口类型 |
| CT-13 | P3 | 20–22 | `BuildOptions.CertManager any` 靠注释说明类型，应抽独立接口避免循环依赖 |

**测试缺口**：Start→Stop 竞态（Starting 窗口期）；Restart 后旧 StopChan 引用行为；LoadFromConfig 重复加载同一 Type。

---

## S4a — containers/xray 核心（进程管理 + 配置渲染）

### `pkg/proxy/containers/xray/exec_runner.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| ~~X-01~~ | ✅ 已修复 | 1002–1014 | ~~`XrayInbound.addedUsers` map 在 reconcile goroutine 和 event handler goroutine 中并发读写，无任何锁保护，data race~~（加 mutex + 封装为 hasAddedUser/listAddedUsers/markAddedUser/unmarkAddedUser 私有方法） |
| X-02 | P1 | 374 | `generateDefaultConfig()` 硬编码 APIPort `62789`，与 `NewExecutor` 动态分配的端口不一致，grpcAPIAddress 与配置文件端口可能不匹配 |
| X-03 | P1 | — | xray 子进程崩溃后 `IsRunning()` 仍返回 true，无 watchdog，gRPC 调用静默失败，无自愈机制 |
| X-04 | P2 | 481 | `Restart()` 绕过 BaseContainer 状态机，不停/重启 reconcileLoop；`Reload()` 同样不重启 reconcileLoop |
| X-05 | P2 | 526 vs 802 | `RemoveInboundConfig` 删 store，`RemoveInboundNative` 不删 store，行为不一致，调用方容易选错导致 store/内存不一致 |
| X-06 | P3 | 501 | `Version()` 每次调用都启动子进程，无缓存，高频调用会产生大量子进程 |

### `pkg/proxy/containers/xray/grpc_client.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| X-07 | P0 | 387 | `QueryStats` 把 JSON 序列化结果当 protobuf wire format 发送，完全错误，实际运行大概率返回乱码/错误 |
| X-08 | P2 | 84 | 每次调用新建 gRPC 连接；`AddInbound` 有 500ms 硬编码 sleep |
| X-09 | P3 | 116 | debug 分支使用 `fmt.Printf` 而非结构化日志 |
| X-10 | P3 | 294 | `AddUser` 硬编码 Trojan account 类型，注释也说"currently unused" |

### `pkg/proxy/containers/xray/config_renderer.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| X-11 | P3 | 156 | `buildAPI()` 的 `port` 参数完全未使用 |

### `pkg/proxy/containers/xray/updater.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| X-12 | P2 | 187–193 | Rollback 失败时错误被忽略，可能留下"新版本已替换但进程用旧版本"的不一致状态 |
| X-13 | P3 | 213 | zip 解压无大小限制，有 zip-bomb 风险 |
| X-14 | P3 | — | 成功更新后 backupPath 未清理，频繁更新会积累旧文件 |

### `pkg/proxy/containers/xray/register.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| X-15 | P3 | 39 | `AutoDownload` 默认 `true`，生产环境可能意外触发自动下载 |

---

## S4b — containers/xray InboundAdapter + profilegen

### `pkg/proxy/containers/xray/inbound_adapter.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| ~~A-01~~ | ❌ 误报 | 136 | ~~`port` 被 `fmt.Sprintf("%d", ...)` 转为字符串写入 JSON~~（xray 配置中 `port` 字段支持字符串形式的 port range 表达式，原写法正确） |
| A-02 | P1 | 534–536 | Reality `shortId`（单数 key）xray 不识别，应改为 `shortIds: []string{...}` |
| A-03 | P1 | 579–583 | 自动生成的 `realityPublicKey` 未回写 `extensions`，订阅生成时找不到公钥 |
| A-04 | P2 | 162–175 | Shadowsocks 多用户被静默截断为第一个用户，无警告 |
| A-05 | P2 | 56–57 | email 字段策略各 profilegen 自行硬编码（`auto@vmess.local` 等），无统一格式，xray stats email 可能冲突 |
| A-06 | P3 | — | Extensions key 无常量定义，30+ 魔法字符串分散在多个文件 |

### `pkg/proxy/containers/xray/profilegen/vless_profiles.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| V-01 | P0 | 279 | `generateRandomKeyPair` 将私钥当公钥返回，私钥泄露到订阅链接的 `pbk=` 参数，Reality 握手必然失败 |
| V-02 | P2 | 101 | h3 transport 写入 xray 配置，xray 不支持 `network: "h3"` |
| V-03 | P3 | 146–157 | `normalizeTransport` 函数体所有分支都 `return t`，是空操作 |
| V-04 | P2 | 122–124 | 空 security 通过 Validate 后被 `applyDefaults` 静默改为 `"tls"`，行为对调用方不透明 |

### `pkg/proxy/containers/xray/subscription.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| ~~S-01~~ | ❌ 误报 | 355 | ~~VMess URI 使用 `RawURLEncoding`，主流客户端期望 `StdEncoding`~~（review agent 误判，原写法正确） |
| S-02 | P0 | 254–259 | `extString` 无法读取 `[]string` 类型的 `reality_server_names`/`reality_short_ids`，Reality URI 的 `serverNames`/`sid` 参数永远为空 |
| S-03 | P3 | 188–213 | `generateSubscriptionSpec` 及其内部 `buildSubscriptionExtensions` 是死代码（实际走 `XrayInbound.GetSub` 路径） |

### `pkg/proxy/containers/xray/profilegen/shadowsocks.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| SS-01 | P2 | 88 | SS + Reality 组合未拦截，是 xray 不支持的非法配置 |
| SS-02 | P2 | 199 | `validate2022Password` 只接受带 padding base64，误拒合法的 no-padding 密钥 |

### `pkg/proxy/containers/xray/profilegen/trojan_tls.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| T-01 | P3 | 84–88 | 密码用 base64 含 `+`/`/`/`=`，部分客户端 URI 解析会出问题，建议改 hex |

---

## S5 — containers/hysteria + containers/snell

### 两者共有

| ID | 优先级 | 位置 | 问题 |
|----|--------|------|------|
| H-01 | P2 | hysteria:141 / snell:102 | `Subscribe()` 注册了 subscriber channel，`stop` 时从未调用 `Unsubscribe()`，UserManager 永久持有已销毁容器的 channel 引用 |
| H-02 | P2 | 两者 | `forwardUserEvents` goroutine 监听外部 source channel，`closeUserEventCh()` 只关闭本地转发 channel，goroutine 泄漏 |
| H-03 | P3 | 两者 | `FastAddInbound` 返回自定义字符串而非 `container.ErrNotSupported`，调用方无法用 `errors.Is` 判断 |

### Hysteria2 特有

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| H-04 | P0 | container.go:63,226 | `certWaitStopCh` 字段在 run goroutine 和 stop 函数中无锁并发读写，data race |
| H-05 | P1 | container.go:66 | run 函数立即返回 nil → `MarkRunning()` 立即执行，但实际进程启动是异步的；`startProcess()` 失败时只打日志，`IsRunning()` 仍返回 true |
| H-06 | P3 | process.go:127–128 | `httpPort == 0` 时生成 `http://127.0.0.1:0/...`，认证静默失败，无校验保护 |
| H-07 | P3 | container.go:255 | `Reload()` 是 no-op，但 hysteria2 支持 SIGHUP 热重载，行为对调用方不透明 |

### Snell 特有

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| SN-01 | P0 | container.go:313–373 vs 488–534 | `addedUsers` map 在 `handleUserEvent` 和 `reconcileUsers` goroutine 中并发读写，无 mutex 保护，data race |
| SN-02 | P2 | container.go:429–438 | `restoreInboundConfig` 无条件覆盖 `port`/`listen`（Hysteria 有 `if cfg.Port == 0` 守护），用户修改配置后重启仍使用旧端口 |
| SN-03 | P2 | container.go:455 | `GetUserSubscriptions` 中 `sc.userMgr.GetUserPortByDst(...)` 若 `userMgr == nil` 会 nil pointer panic |
| SN-04 | P3 | config.go:23 | 默认 `Listen = "127.0.0.1"`，外部无法直连，应有注释说明配套 forward 层使用的设计意图 |

### 接口层共性

| ID | 优先级 | 问题 |
|----|--------|------|
| H-08 | P2 | 单 inbound 约束完全隐式，`Container` 接口无法在运行前感知，建议增加 `IsSingleInbound() bool` 或在 ContainerType 元数据中表达 |

---

## S6 — usermanager

### `pkg/proxy/usermanager/usermanager.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| U-01 | P0 | 1957–1988,1890 | `onCollect` goroutine 中 `lastSeen` map 并发读写（`ForceCollect` 与定时器并发时触发 data race） |
| U-02 | P1 | 2077–2089 | `GetAllDeltaTraffic(reset)` 两次独立加锁之间有窗口期，`collect()` 写入的增量被 reset 清零但未返回，流量漏计 |
| U-03 | P1 | 1315 | `ReleaseBindPort` 调用 `RemoveRulesByUser`（删全部规则）而非 `RemoveRule(single)`，多 inbound 用户调用会同时删掉其他 inbound 的 forward relay |
| U-04 | P2 | 303–334,1265–1326 | `mutateUser`/`ReleaseBindPort` 在持 `m.mu` 写锁时做 `store.Save`（IO）和 `forwardMgr.RemoveRulesByUser`（外部调用），锁粒度太粗 |
| U-05 | P2 | 806 | `GetBindPort` 未检查 `IsDeleting()`，tombstone 用户可被重新分配端口 |
| U-06 | P2 | 913–916,1286–1289 | `ReleaseBindPort` 注释承诺 finalize（物理删除 tombstone），代码是空实现，tombstone 永久留在内存 |
| U-07 | P2 | 957–959 | `RotateUserPort` 中间状态未 persist，崩溃后 DB 记录旧 `PortMappings`，重启时 forward relay 尝试复用无效旧端口 |
| U-08 | P3 | 547–549 | `GetUser` 对 `deleting` 用户返回 `ErrUserNotFound`，调用方无法区分"用户不存在"和"删除中" |
| U-09 | P3 | 2099 | `ResetUserTotalTraffic` 有 TODO：只清内存不写 DB，重启后从旧值重新累加 |

### `pkg/proxy/usermanager/sync/`

| ID | 优先级 | 文件:行 | 问题 |
|----|--------|---------|------|
| U-10 | P3 | hash.go:17 | `ComputeHash` 包含 `UpdatedAtUs`，节点时钟偏差时相同逻辑修改产生不同 hash，触发不必要 sync |
| U-11 | P3 | hash.go:58–62 | `ComputeHash` 条件包含 `LoginPassword`（非空才写），"清除密码"场景 hash 无感知 |
| U-12 | P3 | version.go:10–15 | `IsNewer` tie-break 用 `OriginNode` 字典序，node-B 永远胜过 node-A，语义任意，应在 DECISIONS.md 中记录 |

---

## S7 — core/subscription + converter + appconfig

### `pkg/proxy/core/subscription/manager.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| SUB-01 | P2 | 57–59 | per-container 错误静默丢弃（`continue` 无日志），用户节点数量静默减少，运维无从排障 |

### `pkg/proxy/core/subscription/converter/clash.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| SUB-02 | P1 | 17–22 | hardcode 四个第三方 subconverter URL，网络不可用时 Clash 订阅完全失败；已有 `buildLocalConfig` 本地降级路径却完全未被调用 |
| SUB-03 | P1 | 17–22 | 用户节点 URI 经 URL encode 后发送给第三方服务，存在隐私泄露风险 |

### `pkg/proxy/core/subscription/converter/http.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| SUB-04 | P1 | — | 无超时、无响应体大小限制（`io.ReadAll`），外部 subconverter 慢响应时整个订阅链路挂起；`subscription/http.go` 已有 10s 超时 + 4MB 限制，两套实现重复且不一致 |

### `pkg/proxy/core/subscription/uri_convert.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| SUB-05 | P1 | 223–228 | `parseVMessURI` 不解析 base64 JSON body，Host/Port/Password 全为零值，Clash/Surge 生成节点 server/port 为空 |
| SUB-06 | P2 | 293–298 | `parseSnellURI` 是死代码，从未被调用 |
| SUB-07 | P3 | 244–260 | SS URI 的 userinfo 经 `url.Parse` 拆分，base64 含 `%3A` 时可能歧义 |

### `pkg/proxy/core/subscription/fake.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| SUB-08 | P2 | 19 | `GenerateFakeSSSub` 在代码库中未被任何地方调用，是死代码 |
| SUB-09 | P3 | 29 | 使用 `time.Now().UnixNano()` 做伪随机种子，密码学不安全 |

### `pkg/proxy/appconfig/`

| ID | 优先级 | 文件:行 | 问题 |
|----|--------|---------|------|
| APP-01 | P2 | loader.go:246–283 | `Validate` 未检查 `rpc_port`/`http_port`/`proxy_host`（空时生成无效订阅 URI）/`cluster.token`（空 token 无认证） |
| APP-02 | P3 | loader.go:196–206 | JWT secret 每次重启重新生成，使所有现存 session 失效，只打 stderr 警告但不持久化 |
| APP-03 | P3 | config.go:207–218 | forward 端口范围与 inbound 端口范围可能冲突，`Validate` 未检查 |

### `pkg/proxy/core/subscription/converter/registry.go`

| ID | 优先级 | 行号 | 问题 |
|----|--------|------|------|
| SUB-10 | P3 | 9–22 | `Register`/`Get`/`GetOrDefault` 只是对 `subscription` 包同名函数的无意义转发，造成调用方困惑 |

---

## 已知待处理问题（非 review 发现，后续标记）

| ID | 优先级 | 状态 | 问题 |
|----|--------|------|------|
| COMPAT-01 | Feature | 最后处理 | VLESS 快速添加路径（`FastAddInbound` → `profilegen` → `inbound_adapter`）需适配最新 xray 字段变更。xray 上游再次修改了配置字段名，当前生成的 native JSON 与新版 xray 不兼容，需对照最新 xray 文档逐字段核查并更新。涉及文件：`pkg/proxy/containers/xray/inbound_adapter.go`、`pkg/proxy/containers/xray/profilegen/vless_profiles.go` |

---

## 全局 P0 汇总（需优先修复）

| ID | 文件 | 问题摘要 |
|----|------|---------|
| V-01 | `profilegen/vless_profiles.go:279` | Reality 密钥对：私钥当公钥返回，私钥泄露到订阅链接 |
| S-02 | `containers/xray/subscription.go:254` | Reality URI `serverNames`/`sid` 参数永远为空 |
| A-01 | `containers/xray/inbound_adapter.go:136` | `port` 转字符串，xray 期望整数，配置加载失败 |
| S-01 | `containers/xray/subscription.go:355` | VMess URI 用 `RawURLEncoding`，主流客户端无法解析 |
| X-01 | `containers/xray/exec_runner.go:1002` | `addedUsers` map 无锁并发读写（xray） |
| H-04 | `containers/hysteria/container.go:63` | `certWaitStopCh` 无锁并发读写，data race |
| SN-01 | `containers/snell/container.go:313` | `addedUsers` map 无锁并发读写（snell） |
| U-01 | `usermanager/usermanager.go:1957` | `onCollect` 中 `lastSeen` map data race |
| X-07 | `containers/xray/grpc_client.go:387` | `QueryStats` 用 JSON 当 protobuf wire format，逻辑错误 |
